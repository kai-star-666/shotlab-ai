# ShotLab AI Roadmap

路线图按验收门槛推进，不以页面功能数量作为升级依据。技术发布号 V3.0 对应产品路线 V0.3–V0.5 的首个可运行闭环。

## V0.3 Stability — 已完成

- 固定 CPU PoseLandmarker、固定采样时间、画布尺寸和模型文件；
- 每球创建并关闭独立模型，避免跨分析跟踪状态污染；
- 关键点清洗、短缺失插值、固定 SG 滤波；
- 确定性最低点、出手和随挥阶段及固定 tie-break；
- 稳定 Issue Engine、可信度门槛和低质量拒绝强评价；
- 10 次重复性测试及解码帧、原始关键点、滤波序列、阶段和最终 JSON 分层哈希。

验收结果：固定有效测试视频 10 次运行的阶段采样点完全一致，关键角度最大差值为 0，问题代码与 cue 方向一致率为 100%。

## V0.4 Training Session — MVP 已完成

- 同一 Session 锁定投篮手和距离类别；
- 连续上传 Shot 1/2/3，并切换查看历史球；
- 与上一球比较 improved / stable / worsened / overcorrected；
- 第二球优先验证上一球 cue，避免一次引入大量新问题；
- 两球可生成暂定总结，五球生成正式总结。

下一轮完善重点：用更多真实同机位样本校准“改善”和“过度纠正”容差，不先扩充 UI。

## V0.5 Personal Baseline — MVP 已完成

- 五个中/高可信度样本后计算中位数、IQR 和均值；
- 知识库参考区间与个人稳定区间求交；
- IndexedDB 本机恢复，最多保留 30 个 Session；
- 不长期保存原视频，只保留结构化结果和 320px 缩略图。

下一轮完善重点：历史可比 Session 聚合、同机位条件识别和真实训练效果标签。

## 当前质量门槛 — 进行中

在进入 V0.6 前完成：

1. 真实 iPhone Safari 与微信内置浏览器 H.264 MP4 完整分析验收；
2. 10–30 名篮球爱好者小范围测试；
3. 不同身高、投篮手、距离、服装、光照和机位的数据集回归；
4. 教练或高水平球员复核主要问题方向和训练 cue；
5. 记录 cue 给出后下一球是否改善，形成真实反馈数据；
6. 明确分析耗时、失败率、低质量拒绝率和 Session 完成率。

## V0.6 Shot Style Match — 暂不启动

只有当前质量门槛通过后，才使用 `reference_profiles/*.json` 做可解释的风格向量比较。它只能描述动作风格接近程度，不能宣称技术水平等同球星，也不能改变底层 Issue Engine 结论。

## 后续候选

- V0.7 Share Card：只分享脱敏总结，不默认上传原视频；
- V0.8 User History / Training Profile：在明确账号、隐私、删除和导出策略后再建设；
- V1.0 Small-scale Commercial Product：稳定性、低质量保护、连续训练、移动端体验和真实用户验证全部达标后再考虑。

## 数据与模型边界

当前优先积累视频 ID、原始/滤波关键点、时间戳、阶段、指标、问题、cue、下一球结果和 cue 是否有效。只有获得约 500–5000 条具有可靠阶段与教练标签的高质量样本后，才评估 Phase Detection、Issue Classifier 或 Coaching Recommendation 模型。

