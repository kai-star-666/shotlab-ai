# Project Status

## 当前版本

V3.0 稳定分析与连续训练教练，代码、本地验收和 Netlify 公网回归均已完成。

## 已完成

- 每球独立 CPU 模型、固定帧时间、关键点清洗/插值/SG 滤波和确定性阶段识别；
- 带 issueCode、deadband、可信度门槛、固定排序和训练映射的知识库；
- 连续 Shot 切换、上一球比较、过度纠正保护、≥5 球个人 Baseline；
- IndexedDB 恢复、最多 30 个 Session、本机清理与结束训练；
- 原视频不长期保存，关键帧/真人骨架移入高级分析，火柴人保留主流程；
- 10 次重复性报告、低质量降级、桌面/390px 移动端测试；
- 原有 47 项 Python 测试和扩展后的 24 项 JavaScript 测试通过。

## 浏览器验收边界

- Chromium 桌面与 390px 手机视口已完成完整视频分析与无横向溢出检查；
- Windows Playwright WebKit 可以完成页面布局检查，但其无头环境无法为本地 Blob 视频触发 metadata，因此不能代替真实 iPhone Safari / 微信内置浏览器的视频解码验收；
- 上线前最后一项人工门槛仍是在真实手机微信中选择一个 H.264 MP4，确认能读取时长并完成一球分析。

## 仍是启发式规则

- 低位带宽、出手综合分数和近似并列阈值；
- 关节训练参考区间、deadband、严重程度分段和 coachingPriority；
- 二维躯干倾斜、手腕漂移、近似停顿和拍摄尺度可比阈值；
- cue 与专项训练的映射。

这些规则可以稳定复现，但“稳定”不等于“已被运动科学充分验证”。下一阶段应使用不同球员、机位、距离和教练标注做校准。

## 多球视频下一步

当前 Session 接受逐球视频。若加入一个视频内连续多球，应先用球/手腕分离与下肢加载事件生成互不重叠 Shot Window，再对每个窗口调用同一个确定性管线，最后按现有 `ShotV1` 追加到 Session；不要在 Issue Engine 内混合切球逻辑。
