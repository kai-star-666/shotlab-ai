# ShotLab AI 项目开发规则

## 长期角色

本项目的开发目标不是一次性演示，而是把 ShotLab AI 逐步建设为稳定、可重复、能够连续指导普通篮球爱好者训练的产品。开发时同时考虑全栈实现、计算机视觉、篮球训练逻辑、产品架构和质量保障。

## 不可破坏的架构边界

```text
用户视频
→ 确定性视频预处理
→ MediaPipe Pose
→ 关键点时间序列与质量控制
→ 确定性阶段与生物力学指标
→ Issue Engine
→ Training Session Logic
→ 文本解释与前端展示
```

- 客观数据必须由确定性管线产生。文本模型不得修改角度、阶段、问题代码、排序或 cue 方向。
- 数据不足时降低可信度或拒绝强判断，不伪造手指拨球、三维球路和进球结果。
- 同一输入的稳定性、建议不矛盾和低质量降级，优先级高于新增展示功能。
- 不推倒已工作的产品重做 Demo；所有版本在现有模块边界上渐进演进。
- 暂不训练自有 Pose/大型动作模型。只有积累足够高质量视频、阶段、教练标签和调整结果后再评估。

## 每次开发流程

1. 阅读 `PROJECT_STATUS.md`、`ARCHITECTURE.md`、`ROADMAP.md` 和 `CHANGELOG.md`。
2. 检查 Git 状态，保留用户未提交且与任务无关的改动。
3. 明确本次任务对应的版本阶段，不跨阶段堆功能。
4. 先写或更新测试，再实施行为变更；Bug 必须先定位根因。
5. 运行相关单元测试、回归测试和真实浏览器测试。
6. 更新项目文档，记录启发式边界与尚未验证的设备或数据条件。
7. 通过验证后再提交 Git；测试视频、报告、原始视频和个人训练数据不得进入公开仓库。

## 产品与科学性原则

- 建议使用训练参考区间、相对调整量、趋势和一致性，不把单个角度称为所有人的标准答案。
- 每球最多突出两个优先问题；已经进入目标区间后 cue 必须改为保持，越界时提示纠正过头。
- 个人基线不能把稳定的错误动作固化为正确标准；个人稳定区间与知识库参考区间无有效交集时，继续使用知识库参考。
- 球星风格匹配只能读取稳定结构化特征，不得反向影响基础分析；未经当前阶段验收，不开始该功能。
- 微信 H5 和微信小程序是两个交付边界。小程序 `web-view` 仍受备案域名、主体和微信后台白名单约束。

## 必须维护的文档

- `README.md`：用户入口、运行方式、隐私和科学性边界。
- `PROJECT_STATUS.md`：当前版本、已实现、测试状态、已知问题和唯一下一步。
- `ARCHITECTURE.md`：模块边界、数据流和持久化策略。
- `CHANGELOG.md`：每个已交付版本的实际变化。
- `ROADMAP.md`：阶段门槛、暂不开发内容和商业化前条件。

